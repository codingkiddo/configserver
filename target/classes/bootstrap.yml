spring:
  application:
    name: config-server
  profiles:
    active:
    - native, git
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
        git:
          uri: https://github.com/codingkiddo/config.git
          #searchPaths: licensingservice
    
server:
  port: 8071
  
encrypt:
  key: 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 


#Enable all Spring Boot Actuator endpoints.
management:
  endpoints:
    web:
      exposure:
        include: "*"